# KV Cache 是什么

我一开始理解 KV Cache 的时候很懵。

后来我发现它本质上就是：

- 每一层 Transformer
- 都会缓存历史 token 的 K 和 V
- 下一个 token 来的时候，不需要重新计算前面所有 token 的 K/V

## 为什么它重要

因为生成模型是一个 token 一个 token 地往后生成。

如果每次都把历史重新算一遍，速度会很慢。

## 我的理解

我现在把它想成：

“每一层都在记笔记，后面生成时直接翻旧笔记。”